@model IEnumerable<IEOtherMasterInfoViewModel>
<p class="text-right has-btn-add-new" style="margin-top: 4px;">
    @Html.ActionLink("Thêm mới", "Create", null, new { @class = "btn btn-default btn-warning" })
</p>
<table class="table table-bordered table-striped table-hover dataTable no-footer">
    <thead>
        <tr class="warning">
            <th>
                STT
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IEOtherMasterCode)
            </th>
            <th style="width:60px">
                @Html.DisplayNameFor(model => model.CreatedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WarehouseName)
            </th>
            <th class="text-center">
                @Html.DisplayNameFor(model => model.TotalPrice)
            </th>
            <th class="text-center">
                @Html.DisplayNameFor(model => model.Money)
            </th>
            <th class="actions text-center" style="width:135px;" >@Resources.LanguageResource.Actions</th>
    </thead>
    <tbody>
        @{ int i = 0;}
        @foreach (var item in Model)
        {
            i++;
            <tr>
                <td class="text-center">
                    @i
                </td>

                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.IEOtherMasterCode)
                </td>
                <td class="text-center" style="width:60px">
                    @Html.DisplayFor(modelItem => item.CreatedDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.WarehouseName)
                </td>
                <td class="text-right">
                    @Html.DisplayFor(modelItem => item.TotalPrice)
                </td>
                <td class="text-right">
                    @Html.DisplayFor(modelItem => item.Money)
                </td>
                <td class="text-center" style="width:190px">
                    @Html.ActionLink("Xem", "Details", new { id = @item.IEOtherMasterId }, new { @class = "btn btn-default btn-sm" })
                    @*@Html.ActionLink("Sửa", "Edit", new { id = item.ImportMasterId }, new { @class = "btn btn-default btn-warning btn-sm" })*@
                     <a class="btn btn-default btn-warning btn-sm" href="/IEOtherMasterViewReport/Index/@item.IEOtherMasterId">In</a>
                    <a class = "btn btn-default btn-danger btn-sm btn-xoa"  data-id ="@item.IEOtherMasterId" data-row="@i" data-toggle="modal" data-target="#confirm-delete" >Huỷ</a>
                    <input type="hidden" name="IEOtherMasterCode" id="IEOtherMasterCode_@i" data-row="@i" value="@item.IEOtherMasterCode" />
                    <input type="hidden" id="idDelete" />
                </td>
            </tr>
        }   
    </tbody>
</table>
<!--Alert_Popup_Delete-->
<div class="modal fade" id="confirm-delete" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                <h4 class="modal-title" id="myModalLabel">Xác nhận xoá đơn hàng</h4>
            </div>
            <div class="modal-body">
                <p>Bạn muốn xoá đơn hàng : <strong></strong> ? </p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Huỷ bỏ</button>
                <button type="button" class="btn btn-danger btn-ok">Đồng ý</button>
            </div>
        </div>
    </div>
</div>
